import { createTheme } from "@mui/material/styles";
const theme = createTheme({
  palette: {
    primary: {
      main: "#0052cc",
    },
    secondary: {
      main: "#00FF7F",
    },
    background: {
      default: "#F5f5f5",
    },
  },
});

export default theme;
